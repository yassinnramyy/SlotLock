{
  "info": {
    "name": "SlotLock",
    "description": "Auth, Tenant (masterdata), Resource/Slot, and Booking endpoints for SlotLock. Run requests top-to-bottom on a fresh DB — each folder's test scripts auto-capture tokens/ids into collection variables used by later requests. The Bookings folder exercises the intentionally naive (no locking) check-then-act booking flow; Cleanup runs last so it doesn't tear down state earlier folders depend on.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    { "key": "baseUrl", "value": "http://localhost:8080" },
    { "key": "superAdminToken", "value": "" },
    { "key": "adminToken", "value": "" },
    { "key": "staffToken", "value": "" },
    { "key": "customerToken", "value": "" },
    { "key": "tenantId", "value": "" },
    { "key": "tenantSlug", "value": "acme-clinic" },
    { "key": "resourceId", "value": "" },
    { "key": "slotsFrom", "value": "2026-07-20" },
    { "key": "slotsTo", "value": "2026-07-27" },
    { "key": "slotId", "value": "" },
    { "key": "slotId2", "value": "" },
    { "key": "bookingId", "value": "" },
    { "key": "bookingId2", "value": "" },
    { "key": "customer2Token", "value": "" }
  ],
  "item": [
    {
      "name": "Auth",
      "item": [
        {
          "name": "Register - Bootstrap SUPER_ADMIN",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.response.code === 201) {",
                  "    const body = pm.response.json();",
                  "    pm.collectionVariables.set('superAdminToken', body.token);",
                  "    pm.test('created SUPER_ADMIN', () => pm.expect(body.role).to.eql('SUPER_ADMIN'));",
                  "} else {",
                  "    pm.test('bootstrap already completed (expected on repeat runs)', () => pm.expect(pm.response.code).to.eql(403));",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [ { "key": "Content-Type", "value": "application/json" } ],
            "url": { "raw": "{{baseUrl}}/api/auth/register", "host": [ "{{baseUrl}}" ], "path": [ "api", "auth", "register" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"super@example.com\",\n  \"password\": \"Password123\",\n  \"role\": \"SUPER_ADMIN\"\n}"
            },
            "description": "Only succeeds once per system — the very first SUPER_ADMIN account. A second attempt returns 403 BUSINESS_RULE_VIOLATION-style ACCESS_DENIED."
          }
        },
        {
          "name": "Register - Second SUPER_ADMIN attempt (expect 403)",
          "request": {
            "method": "POST",
            "header": [ { "key": "Content-Type", "value": "application/json" } ],
            "url": { "raw": "{{baseUrl}}/api/auth/register", "host": [ "{{baseUrl}}" ], "path": [ "api", "auth", "register" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"super2@example.com\",\n  \"password\": \"Password123\",\n  \"role\": \"SUPER_ADMIN\"\n}"
            },
            "description": "Demonstrates the bootstrap lock — should always return 403 once a SUPER_ADMIN exists."
          }
        },
        {
          "name": "Register - Self-registered ADMIN attempt (expect 403)",
          "request": {
            "method": "POST",
            "header": [ { "key": "Content-Type", "value": "application/json" } ],
            "url": { "raw": "{{baseUrl}}/api/auth/register", "host": [ "{{baseUrl}}" ], "path": [ "api", "auth", "register" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"sneaky-admin@example.com\",\n  \"password\": \"Password123\",\n  \"role\": \"ADMIN\"\n}"
            },
            "description": "ADMIN/STAFF can never self-register — always 403. Use 'Create tenant admin' or 'Create staff' instead."
          }
        },
        {
          "name": "Register - CUSTOMER",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.response.code === 201) {",
                  "    pm.collectionVariables.set('customerToken', pm.response.json().token);",
                  "}",
                  "pm.test('status is 201', () => pm.response.to.have.status(201));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [ { "key": "Content-Type", "value": "application/json" } ],
            "url": { "raw": "{{baseUrl}}/api/auth/register", "host": [ "{{baseUrl}}" ], "path": [ "api", "auth", "register" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"customer1@example.com\",\n  \"password\": \"Password123\"\n}"
            },
            "description": "role omitted -> always defaults to CUSTOMER with no tenant."
          }
        },
        {
          "name": "Login - SUPER_ADMIN",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.response.code === 200) {",
                  "    pm.collectionVariables.set('superAdminToken', pm.response.json().token);",
                  "}",
                  "pm.test('status is 200', () => pm.response.to.have.status(200));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [ { "key": "Content-Type", "value": "application/json" } ],
            "url": { "raw": "{{baseUrl}}/api/auth/login", "host": [ "{{baseUrl}}" ], "path": [ "api", "auth", "login" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"super@example.com\",\n  \"password\": \"Password123\"\n}"
            }
          }
        },
        {
          "name": "Login - CUSTOMER",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.response.code === 200) {",
                  "    pm.collectionVariables.set('customerToken', pm.response.json().token);",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [ { "key": "Content-Type", "value": "application/json" } ],
            "url": { "raw": "{{baseUrl}}/api/auth/login", "host": [ "{{baseUrl}}" ], "path": [ "api", "auth", "login" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"customer1@example.com\",\n  \"password\": \"Password123\"\n}"
            }
          }
        },
        {
          "name": "Create tenant admin (SUPER_ADMIN only)",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 201', () => pm.response.to.have.status(201));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{superAdminToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/auth/admins", "host": [ "{{baseUrl}}" ], "path": [ "api", "auth", "admins" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"tenant-admin@example.com\",\n  \"password\": \"Password123\",\n  \"tenantId\": {{tenantId}}\n}"
            },
            "description": "Requires {{tenantId}} to be set — run 'Tenants > Create tenant' first."
          }
        },
        {
          "name": "Create tenant admin - non-super-admin (expect 403)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{customerToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/auth/admins", "host": [ "{{baseUrl}}" ], "path": [ "api", "auth", "admins" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"sneaky@example.com\",\n  \"password\": \"Password123\",\n  \"tenantId\": {{tenantId}}\n}"
            }
          }
        },
        {
          "name": "Login - tenant ADMIN",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.response.code === 200) {",
                  "    pm.collectionVariables.set('adminToken', pm.response.json().token);",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [ { "key": "Content-Type", "value": "application/json" } ],
            "url": { "raw": "{{baseUrl}}/api/auth/login", "host": [ "{{baseUrl}}" ], "path": [ "api", "auth", "login" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"tenant-admin@example.com\",\n  \"password\": \"Password123\"\n}"
            }
          }
        },
        {
          "name": "Create staff (ADMIN only, own tenant)",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 201', () => pm.response.to.have.status(201));",
                  "if (pm.response.code === 201) {",
                  "    pm.test('tenantId matches admin\\'s own tenant', () => pm.expect(pm.response.json().tenantId).to.eql(Number(pm.collectionVariables.get('tenantId'))));",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{adminToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/auth/staff", "host": [ "{{baseUrl}}" ], "path": [ "api", "auth", "staff" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"tenant-staff@example.com\",\n  \"password\": \"Password123\"\n}"
            },
            "description": "tenantId is never sent by the client — it's derived from the ADMIN's own JWT."
          }
        },
        {
          "name": "Create staff - non-admin (expect 403)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{customerToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/auth/staff", "host": [ "{{baseUrl}}" ], "path": [ "api", "auth", "staff" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"sneaky-staff@example.com\",\n  \"password\": \"Password123\"\n}"
            }
          }
        }
      ]
    },
    {
      "name": "Tenants",
      "item": [
        {
          "name": "Create tenant (SUPER_ADMIN only)",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 201', () => pm.response.to.have.status(201));",
                  "if (pm.response.code === 201) {",
                  "    pm.collectionVariables.set('tenantId', pm.response.json().id);",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{superAdminToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/tenants", "host": [ "{{baseUrl}}" ], "path": [ "api", "tenants" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Acme Clinic\",\n  \"slug\": \"{{tenantSlug}}\",\n  \"category\": \"CLINIC\"\n}"
            }
          }
        },
        {
          "name": "Create tenant - non-super-admin (expect 403)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{customerToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/tenants", "host": [ "{{baseUrl}}" ], "path": [ "api", "tenants" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Should Fail\",\n  \"slug\": \"should-fail\",\n  \"category\": \"OTHER\"\n}"
            }
          }
        },
        {
          "name": "Create tenant - duplicate slug (expect 409)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{superAdminToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/tenants", "host": [ "{{baseUrl}}" ], "path": [ "api", "tenants" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Duplicate\",\n  \"slug\": \"{{tenantSlug}}\",\n  \"category\": \"CLINIC\"\n}"
            }
          }
        },
        {
          "name": "List all tenants (SUPER_ADMIN only)",
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{superAdminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/tenants", "host": [ "{{baseUrl}}" ], "path": [ "api", "tenants" ] }
          }
        },
        {
          "name": "Get tenant by id (any authenticated role)",
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/tenants/{{tenantId}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "tenants", "{{tenantId}}" ] }
          }
        },
        {
          "name": "Get tenant by id - no token (expect 401)",
          "request": {
            "method": "GET",
            "url": { "raw": "{{baseUrl}}/api/tenants/{{tenantId}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "tenants", "{{tenantId}}" ] }
          }
        },
        {
          "name": "Get tenant by slug (public, no auth)",
          "request": {
            "method": "GET",
            "url": { "raw": "{{baseUrl}}/api/tenants/slug/{{tenantSlug}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "tenants", "slug", "{{tenantSlug}}" ] }
          }
        },
        {
          "name": "Update tenant (SUPER_ADMIN only)",
          "request": {
            "method": "PUT",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{superAdminToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/tenants/{{tenantId}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "tenants", "{{tenantId}}" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Acme Clinic (Renamed)\",\n  \"slug\": \"{{tenantSlug}}\",\n  \"category\": \"CLINIC\"\n}"
            }
          }
        },
        {
          "name": "Delete tenant - with users assigned (expect 409)",
          "request": {
            "method": "DELETE",
            "header": [ { "key": "Authorization", "value": "Bearer {{superAdminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/tenants/{{tenantId}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "tenants", "{{tenantId}}" ] },
            "description": "Fails cleanly with BUSINESS_RULE_VIOLATION once the tenant has an admin/staff assigned via the Auth folder requests above."
          }
        },
        {
          "name": "Delete tenant - not found (expect 404)",
          "request": {
            "method": "DELETE",
            "header": [ { "key": "Authorization", "value": "Bearer {{superAdminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/tenants/999999", "host": [ "{{baseUrl}}" ], "path": [ "api", "tenants", "999999" ] }
          }
        }
      ]
    },
    {
      "name": "Resources",
      "item": [
        {
          "name": "Create resource with availability windows (ADMIN only)",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 201', () => pm.response.to.have.status(201));",
                  "if (pm.response.code === 201) {",
                  "    pm.collectionVariables.set('resourceId', pm.response.json().id);",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{adminToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/resources", "host": [ "{{baseUrl}}" ], "path": [ "api", "resources" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Dr. Smith - Consultation Room\",\n  \"description\": \"General consultations\",\n  \"slotDurationMinutes\": 30,\n  \"availabilityWindows\": [\n    { \"dayOfWeek\": 1, \"startTime\": \"09:00:00\", \"endTime\": \"12:00:00\" },\n    { \"dayOfWeek\": 3, \"startTime\": \"09:00:00\", \"endTime\": \"12:00:00\" }\n  ]\n}"
            },
            "description": "dayOfWeek uses 0=Sunday..6=Saturday. Example: Monday + Wednesday, 9am-12pm, 30-minute slots. tenantId is never sent — it's derived from the ADMIN's own JWT."
          }
        },
        {
          "name": "Create resource - non-admin (expect 403)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{customerToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/resources", "host": [ "{{baseUrl}}" ], "path": [ "api", "resources" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Should Fail\",\n  \"availabilityWindows\": [\n    { \"dayOfWeek\": 1, \"startTime\": \"09:00:00\", \"endTime\": \"10:00:00\" }\n  ]\n}"
            }
          }
        },
        {
          "name": "Create resource - SUPER_ADMIN (expect 403)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{superAdminToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/resources", "host": [ "{{baseUrl}}" ], "path": [ "api", "resources" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Should Fail\",\n  \"availabilityWindows\": [\n    { \"dayOfWeek\": 1, \"startTime\": \"09:00:00\", \"endTime\": \"10:00:00\" }\n  ]\n}"
            },
            "description": "SUPER_ADMIN is scoped to tenant/admin management only, not resources/bookings — always 403 here."
          }
        },
        {
          "name": "List resources for current tenant",
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/resources", "host": [ "{{baseUrl}}" ], "path": [ "api", "resources" ] }
          }
        },
        {
          "name": "Get resource by id",
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/resources/{{resourceId}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "resources", "{{resourceId}}" ] }
          }
        },
        {
          "name": "Get resource by id - other tenant's id (expect 404)",
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/resources/999999", "host": [ "{{baseUrl}}" ], "path": [ "api", "resources", "999999" ] },
            "description": "Deliberately 404 rather than 403 — never reveal that an id belongs to someone else's tenant."
          }
        },
        {
          "name": "Generate + get slots for date range",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.response.code === 200) {",
                  "    const slots = pm.response.json();",
                  "    const open = slots.filter(s => s.status === 'OPEN');",
                  "    const currentSlotId = pm.collectionVariables.get('slotId');",
                  "    if (!currentSlotId && open.length > 0) {",
                  "        pm.collectionVariables.set('slotId', open[0].id);",
                  "    } else if (currentSlotId && open.length > 0) {",
                  "        const next = open.find(s => String(s.id) !== String(currentSlotId));",
                  "        if (next) { pm.collectionVariables.set('slotId2', next.id); }",
                  "    }",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": {
              "raw": "{{baseUrl}}/api/resources/{{resourceId}}/slots?from={{slotsFrom}}&to={{slotsTo}}",
              "host": [ "{{baseUrl}}" ],
              "path": [ "api", "resources", "{{resourceId}}", "slots" ],
              "query": [
                { "key": "from", "value": "{{slotsFrom}}" },
                { "key": "to", "value": "{{slotsTo}}" }
              ]
            },
            "description": "First call generates slots for every availability window occurrence in [from, to] and returns them. Run this request again with the same range to confirm it's idempotent — same slot ids come back, nothing is duplicated. Also captures slotId (first OPEN slot) into a collection variable for the Bookings folder; re-running it after a booking captures slotId2 as a second, still-OPEN slot."
          }
        },
        {
          "name": "Generate slots - range exceeds 90 days (expect 400)",
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": {
              "raw": "{{baseUrl}}/api/resources/{{resourceId}}/slots?from={{slotsFrom}}&to=2026-12-01",
              "host": [ "{{baseUrl}}" ],
              "path": [ "api", "resources", "{{resourceId}}", "slots" ],
              "query": [
                { "key": "from", "value": "{{slotsFrom}}" },
                { "key": "to", "value": "2026-12-01" }
              ]
            }
          }
        },
        {
          "name": "Generate slots - to before from (expect 400)",
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": {
              "raw": "{{baseUrl}}/api/resources/{{resourceId}}/slots?from={{slotsTo}}&to={{slotsFrom}}",
              "host": [ "{{baseUrl}}" ],
              "path": [ "api", "resources", "{{resourceId}}", "slots" ],
              "query": [
                { "key": "from", "value": "{{slotsTo}}" },
                { "key": "to", "value": "{{slotsFrom}}" }
              ]
            }
          }
        },
        {
          "name": "Get slots - non-admin/staff/customer (expect 403)",
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{superAdminToken}}" } ],
            "url": {
              "raw": "{{baseUrl}}/api/resources/{{resourceId}}/slots?from={{slotsFrom}}&to={{slotsTo}}",
              "host": [ "{{baseUrl}}" ],
              "path": [ "api", "resources", "{{resourceId}}", "slots" ],
              "query": [
                { "key": "from", "value": "{{slotsFrom}}" },
                { "key": "to", "value": "{{slotsTo}}" }
              ]
            },
            "description": "SUPER_ADMIN has no tenant, so booking endpoints are off-limits — 403."
          }
        },
        {
          "name": "Update resource (ADMIN only)",
          "request": {
            "method": "PUT",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{adminToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/resources/{{resourceId}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "resources", "{{resourceId}}" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Dr. Smith - Renamed Room\",\n  \"description\": \"Updated hours\",\n  \"slotDurationMinutes\": 20,\n  \"availabilityWindows\": [\n    { \"dayOfWeek\": 2, \"startTime\": \"13:00:00\", \"endTime\": \"15:00:00\" },\n    { \"dayOfWeek\": 4, \"startTime\": \"13:00:00\", \"endTime\": \"15:00:00\" }\n  ]\n}"
            },
            "description": "Full replace, same shape as create — name/description/slotDurationMinutes are overwritten and all existing availability windows are replaced with the ones sent here. Already-generated slots are left untouched."
          }
        },
        {
          "name": "Update resource - non-admin (expect 403)",
          "request": {
            "method": "PUT",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{customerToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/resources/{{resourceId}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "resources", "{{resourceId}}" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Should Fail\",\n  \"availabilityWindows\": [\n    { \"dayOfWeek\": 1, \"startTime\": \"09:00:00\", \"endTime\": \"10:00:00\" }\n  ]\n}"
            }
          }
        },
        {
          "name": "Update resource - not found (expect 404)",
          "request": {
            "method": "PUT",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{adminToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/resources/999999", "host": [ "{{baseUrl}}" ], "path": [ "api", "resources", "999999" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Should Fail\",\n  \"availabilityWindows\": [\n    { \"dayOfWeek\": 1, \"startTime\": \"09:00:00\", \"endTime\": \"10:00:00\" }\n  ]\n}"
            }
          }
        },
        {
          "name": "Delete resource - non-admin (expect 403)",
          "request": {
            "method": "DELETE",
            "header": [ { "key": "Authorization", "value": "Bearer {{customerToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/resources/{{resourceId}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "resources", "{{resourceId}}" ] }
          }
        },
        {
          "name": "Delete resource - not found (expect 404)",
          "request": {
            "method": "DELETE",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/resources/999999", "host": [ "{{baseUrl}}" ], "path": [ "api", "resources", "999999" ] }
          }
        }
      ]
    },
    {
      "name": "Bookings",
      "description": "Naive (deliberately unsafe) booking flow — no row-locking, plain check-then-act on Slot.status. Run top-to-bottom; each step depends on collection variables (slotId/slotId2/bookingId/bookingId2) captured by earlier steps and by 'Resources > Generate + get slots for date range'.",
      "item": [
        {
          "name": "Register - second CUSTOMER",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.response.code === 201) {",
                  "    pm.collectionVariables.set('customer2Token', pm.response.json().token);",
                  "}",
                  "pm.test('status is 201', () => pm.response.to.have.status(201));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [ { "key": "Content-Type", "value": "application/json" } ],
            "url": { "raw": "{{baseUrl}}/api/auth/register", "host": [ "{{baseUrl}}" ], "path": [ "api", "auth", "register" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"customer2@example.com\",\n  \"password\": \"Password123\"\n}"
            },
            "description": "A second, unrelated CUSTOMER used to prove ownership checks on GET/cancel/delete."
          }
        },
        {
          "name": "Book slot (CUSTOMER, expect 201)",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 201', () => pm.response.to.have.status(201));",
                  "if (pm.response.code === 201) {",
                  "    const body = pm.response.json();",
                  "    pm.collectionVariables.set('bookingId', body.id);",
                  "    pm.test('status is CONFIRMED', () => pm.expect(body.status).to.eql('CONFIRMED'));",
                  "    pm.test('slotId matches request', () => pm.expect(body.slotId).to.eql(Number(pm.collectionVariables.get('slotId'))));",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{customerToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/bookings", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"slotId\": {{slotId}},\n  \"idempotencyKey\": \"postman-key-A\"\n}"
            },
            "description": "Requires {{slotId}} — run 'Resources > Generate + get slots for date range' first."
          }
        },
        {
          "name": "Book same slot - different idempotency key (expect 409)",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 409', () => pm.response.to.have.status(409));",
                  "if (pm.response.code === 409) {",
                  "    pm.test('errorCode is SLOT_CONFLICT', () => pm.expect(pm.response.json().errorCode).to.eql('SLOT_CONFLICT'));",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{customerToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/bookings", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"slotId\": {{slotId}},\n  \"idempotencyKey\": \"postman-key-B\"\n}"
            },
            "description": "Same slot as the successful booking above, but a fresh idempotency key — the naive check-then-act sees the slot is already BOOKED and rejects it with 409."
          }
        },
        {
          "name": "Retry original booking request - same idempotency key (expect 200, no duplicate)",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "if (pm.response.code === 200) {",
                  "    pm.test('returns the same booking id, not a new one', () => pm.expect(pm.response.json().id).to.eql(Number(pm.collectionVariables.get('bookingId'))));",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{customerToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/bookings", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"slotId\": {{slotId}},\n  \"idempotencyKey\": \"postman-key-A\"\n}"
            },
            "description": "Proves idempotency: the same key returns 200 with the original booking instead of re-checking the (now BOOKED) slot."
          }
        },
        {
          "name": "Get booking by id (owner)",
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{customerToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/bookings/{{bookingId}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings", "{{bookingId}}" ] }
          }
        },
        {
          "name": "Get booking by id - non-owner customer (expect 404)",
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{customer2Token}}" } ],
            "url": { "raw": "{{baseUrl}}/api/bookings/{{bookingId}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings", "{{bookingId}}" ] },
            "description": "Deliberately 404 rather than 403 — never reveal that a booking id belongs to someone else."
          }
        },
        {
          "name": "Get booking by id (ADMIN can view any tenant booking)",
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/bookings/{{bookingId}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings", "{{bookingId}}" ] }
          }
        },
        {
          "name": "List bookings (CUSTOMER sees only own)",
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{customerToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/bookings", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings" ] }
          }
        },
        {
          "name": "List bookings (ADMIN sees all tenant bookings)",
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/bookings", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings" ] }
          }
        },
        {
          "name": "Refresh slots - confirm booked, capture a second OPEN slot",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.response.code === 200) {",
                  "    const slots = pm.response.json();",
                  "    const bookedSlot = slots.find(s => String(s.id) === String(pm.collectionVariables.get('slotId')));",
                  "    if (bookedSlot) { pm.test('originally booked slot is now BOOKED', () => pm.expect(bookedSlot.status).to.eql('BOOKED')); }",
                  "    const open = slots.filter(s => s.status === 'OPEN' && String(s.id) !== String(pm.collectionVariables.get('slotId')));",
                  "    if (open.length > 0) { pm.collectionVariables.set('slotId2', open[0].id); }",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": {
              "raw": "{{baseUrl}}/api/resources/{{resourceId}}/slots?from={{slotsFrom}}&to={{slotsTo}}",
              "host": [ "{{baseUrl}}" ],
              "path": [ "api", "resources", "{{resourceId}}", "slots" ],
              "query": [
                { "key": "from", "value": "{{slotsFrom}}" },
                { "key": "to", "value": "{{slotsTo}}" }
              ]
            }
          }
        },
        {
          "name": "Book second slot (CUSTOMER, expect 201)",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 201', () => pm.response.to.have.status(201));",
                  "if (pm.response.code === 201) {",
                  "    pm.collectionVariables.set('bookingId2', pm.response.json().id);",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{customerToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/bookings", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"slotId\": {{slotId2}},\n  \"idempotencyKey\": \"postman-key-C\"\n}"
            },
            "description": "Left CONFIRMED (not cancelled yet) on purpose, to exercise the delete-guard tests below."
          }
        },
        {
          "name": "Delete booking - still CONFIRMED (expect 409)",
          "request": {
            "method": "DELETE",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/bookings/{{bookingId2}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings", "{{bookingId2}}" ] },
            "description": "Hard delete is only allowed for CANCELLED bookings — this one hasn't been cancelled yet."
          }
        },
        {
          "name": "Delete booking - non-admin (expect 403)",
          "request": {
            "method": "DELETE",
            "header": [ { "key": "Authorization", "value": "Bearer {{customerToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/bookings/{{bookingId}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings", "{{bookingId}}" ] }
          }
        },
        {
          "name": "Cancel booking (owner)",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "if (pm.response.code === 200) {",
                  "    pm.test('status is CANCELLED', () => pm.expect(pm.response.json().status).to.eql('CANCELLED'));",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [ { "key": "Authorization", "value": "Bearer {{customerToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/bookings/{{bookingId}}/cancel", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings", "{{bookingId}}", "cancel" ] }
          }
        },
        {
          "name": "Cancel booking - non-owner non-admin (expect 403)",
          "request": {
            "method": "POST",
            "header": [ { "key": "Authorization", "value": "Bearer {{customer2Token}}" } ],
            "url": { "raw": "{{baseUrl}}/api/bookings/{{bookingId2}}/cancel", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings", "{{bookingId2}}", "cancel" ] }
          }
        },
        {
          "name": "Cancel booking (ADMIN can cancel any tenant booking)",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "if (pm.response.code === 200) {",
                  "    pm.test('status is CANCELLED', () => pm.expect(pm.response.json().status).to.eql('CANCELLED'));",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/bookings/{{bookingId2}}/cancel", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings", "{{bookingId2}}", "cancel" ] }
          }
        },
        {
          "name": "Cancel booking - not found (expect 404)",
          "request": {
            "method": "POST",
            "header": [ { "key": "Authorization", "value": "Bearer {{customerToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/bookings/999999/cancel", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings", "999999", "cancel" ] }
          }
        },
        {
          "name": "Book slot - slot not found (expect 404)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{customerToken}}" }
            ],
            "url": { "raw": "{{baseUrl}}/api/bookings", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings" ] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"slotId\": 999999,\n  \"idempotencyKey\": \"postman-key-D\"\n}"
            }
          }
        },
        {
          "name": "Refresh slots - confirm both cancelled bookings reopened their slots",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.response.code === 200) {",
                  "    const slots = pm.response.json();",
                  "    const slotA = slots.find(s => String(s.id) === String(pm.collectionVariables.get('slotId')));",
                  "    const slotB = slots.find(s => String(s.id) === String(pm.collectionVariables.get('slotId2')));",
                  "    if (slotA) { pm.test('slot A is OPEN again after cancel', () => pm.expect(slotA.status).to.eql('OPEN')); }",
                  "    if (slotB) { pm.test('slot B is OPEN again after cancel', () => pm.expect(slotB.status).to.eql('OPEN')); }",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": {
              "raw": "{{baseUrl}}/api/resources/{{resourceId}}/slots?from={{slotsFrom}}&to={{slotsTo}}",
              "host": [ "{{baseUrl}}" ],
              "path": [ "api", "resources", "{{resourceId}}", "slots" ],
              "query": [
                { "key": "from", "value": "{{slotsFrom}}" },
                { "key": "to", "value": "{{slotsTo}}" }
              ]
            }
          }
        },
        {
          "name": "Delete booking (ADMIN, now CANCELLED, expect 204)",
          "request": {
            "method": "DELETE",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/bookings/{{bookingId}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings", "{{bookingId}}" ] }
          }
        },
        {
          "name": "Delete booking - not found (expect 404)",
          "request": {
            "method": "DELETE",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/bookings/999999", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings", "999999" ] }
          }
        },
        {
          "name": "Delete booking (ADMIN, second CANCELLED booking, expect 204)",
          "request": {
            "method": "DELETE",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/bookings/{{bookingId2}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "bookings", "{{bookingId2}}" ] }
          }
        }
      ]
    },
    {
      "name": "Cleanup",
      "description": "Destructive requests that must run last so earlier folders (Bookings especially) still have a live resource/slots to work with.",
      "item": [
        {
          "name": "Delete resource (ADMIN only)",
          "request": {
            "method": "DELETE",
            "header": [ { "key": "Authorization", "value": "Bearer {{adminToken}}" } ],
            "url": { "raw": "{{baseUrl}}/api/resources/{{resourceId}}", "host": [ "{{baseUrl}}" ], "path": [ "api", "resources", "{{resourceId}}" ] },
            "description": "Cascades: deletes the resource's slots and availability windows in the same transaction, then the resource itself. Blocked with 409 if any slot on this resource is BOOKED — all bookings above are cancelled by this point, so this should succeed."
          }
        }
      ]
    }
  ]
}
